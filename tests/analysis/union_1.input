type Maybe('a) =
  | Just('a)
  | None;

#[entry]
fn main() -> () {
  let x = if 0 < 5 {
    Maybe::Just(123)
  } else {
    Maybe::None
  };

  ::Io::println(match x {
    Maybe::Just(s) => "just: " ++ s,
    Maybe::None => "none",
  });
}
