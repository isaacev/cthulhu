use ::Io;
use ::Fmt;
use ::List;

fn loop_body(i: Int, s: Str) -> () {
  Io::println(Fmt::int(i) ++ " " ++ s);
}

#[entry]
fn main() -> () {
  ["Rick", "Morty", "Summer"] |> List::iter_index(loop_body);
}
