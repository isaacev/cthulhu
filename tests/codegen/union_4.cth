use ::Io;

#[entry]
fn main() -> () {
  let x = if 0 < 5 {
    Maybe::Some("hello")
  } else {
    Maybe::None
  };

  Io::println(match x {
    Maybe::Some(s) => s,
    Maybe::None => "none",
  });
}
