use ::Io;

#[entry]
fn main() -> () {
  let x = if 0 < 5 {
    Maybe::Some(123)
  } else {
    Maybe::None
  };

  Io::println(match x {
    Maybe::Some(s) => "just: " ++ s,
    Maybe::None => "none",
  });
}
